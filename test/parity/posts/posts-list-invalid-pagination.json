{
  "id": "posts-list-invalid-pagination",
  "endpoint": "GET /api/posts",
  "description": "Posts list coerces invalid pagination to defaults",
  "request": {
    "method": "GET",
    "path": "/api/posts",
    "query": { "page": -1, "perPage": 999 }
  },
  "expected": {
    "status": 200,
    "contentType": "application/json",
    "body": {
      "items": [],
      "totalCount": 0,
      "page": 1,
      "perPage": 100
    },
    "bodySchema": {
      "type": "object",
      "required": ["items", "totalCount", "page", "perPage"],
      "properties": {
        "items": {
          "type": "array",
          "items": { "$ref": "#/$defs/PostListItem" }
        },
        "totalCount": { "type": "integer", "minimum": 0 },
        "page": { "type": "integer", "minimum": 1 },
        "perPage": { "type": "integer", "minimum": 1, "maximum": 100 }
      },
      "additionalProperties": false,
      "$defs": {
        "PostListItem": {
          "type": "object",
          "required": ["id", "author", "content", "createdAt", "replyCount"],
          "properties": {
            "id": { "type": "string" },
            "author": { "$ref": "#/$defs/PostAuthor" },
            "content": { "type": "string" },
            "createdAt": { "type": "string", "format": "date-time" },
            "replyCount": { "type": "integer", "minimum": 0 }
          },
          "additionalProperties": false
        },
        "PostAuthor": {
          "type": "object",
          "required": ["uid", "username", "avatar", "reputation"],
          "properties": {
            "uid": { "type": "integer", "minimum": 1 },
            "username": { "type": "string" },
            "avatar": { "type": ["string", "null"] },
            "reputation": { "type": "integer", "minimum": 0 }
          },
          "additionalProperties": false
        }
      }
    }
  },
  "notes": "Negative page is coerced to 1. perPage exceeding 100 is clamped to 100. Non-numeric values fall back to defaults (page=1, perPage=20)."
}
