{
  "id": "posts-list-zero-pagination",
  "endpoint": "GET /api/posts",
  "description": "Posts list coerces zero-valued pagination params to defaults",
  "request": {
    "method": "GET",
    "path": "/api/posts",
    "query": { "page": 0, "perPage": 0 }
  },
  "expected": {
    "status": 200,
    "contentType": "application/json",
    "body": {
      "page": 1,
      "perPage": 20
    },
    "bodySchema": {
      "type": "object",
      "required": ["items", "totalCount", "page", "perPage"],
      "properties": {
        "items": {
          "type": "array",
          "items": { "$ref": "#/$defs/PostListItem" }
        },
        "totalCount": { "type": "integer", "minimum": 0 },
        "page": { "type": "integer", "minimum": 1 },
        "perPage": { "type": "integer", "minimum": 1, "maximum": 100 }
      },
      "additionalProperties": false,
      "$defs": {
        "PostListItem": {
          "type": "object",
          "required": ["id", "author", "content", "createdAt", "replyCount"],
          "properties": {
            "id": { "type": "string" },
            "author": { "$ref": "#/$defs/PostAuthor" },
            "content": { "type": "string" },
            "createdAt": { "type": "string", "format": "date-time" },
            "replyCount": { "type": "integer", "minimum": 0 }
          },
          "additionalProperties": false
        },
        "PostAuthor": {
          "type": "object",
          "required": ["uid", "username", "avatar", "reputation"],
          "properties": {
            "uid": { "type": "integer", "minimum": 1 },
            "username": { "type": "string" },
            "avatar": { "type": ["string", "null"] },
            "reputation": { "type": "integer", "minimum": 0 }
          },
          "additionalProperties": false
        }
      }
    }
  },
  "notes": "page=0 and perPage=0 are both falsy, so Number() || default kicks in. page becomes 1, perPage becomes 20."
}
