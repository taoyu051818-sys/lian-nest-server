{
  "id": "category-detail-invalid-cid",
  "endpoint": "GET /api/categories/:cid",
  "description": "Requesting a category with a non-numeric cid returns 404",
  "request": {
    "method": "GET",
    "path": "/api/categories/abc",
    "params": { "cid": "abc" }
  },
  "expected": {
    "status": 404,
    "contentType": "application/json",
    "body": { "statusCode": 404, "error": "Not Found", "code": "NOT_FOUND" },
    "bodySchema": {
      "type": "object",
      "required": ["statusCode", "error", "code"],
      "properties": {
        "statusCode": { "type": "integer", "const": 404 },
        "error": { "type": "string" },
        "code": { "type": "string" }
      },
      "additionalProperties": true
    }
  },
  "notes": "Non-numeric cid string 'abc' fails Number.isInteger check in CategoriesUsecase.getById."
}
