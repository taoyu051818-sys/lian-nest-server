{
  "id": "category-detail-zero-cid",
  "endpoint": "GET /api/categories/:cid",
  "description": "Requesting a category with cid=0 returns 404",
  "request": {
    "method": "GET",
    "path": "/api/categories/0",
    "params": { "cid": "0" }
  },
  "expected": {
    "status": 404,
    "contentType": "application/json",
    "body": { "statusCode": 404, "error": "Not Found", "code": "NOT_FOUND" },
    "bodySchema": {
      "type": "object",
      "required": ["statusCode", "error", "code"],
      "properties": {
        "statusCode": { "type": "integer", "const": 404 },
        "error": { "type": "string" },
        "code": { "type": "string" }
      },
      "additionalProperties": true
    }
  },
  "notes": "cid=0 fails the cid < 1 check in CategoriesUsecase.getById."
}
