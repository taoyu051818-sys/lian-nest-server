{
  "id": "tag-topics-pagination",
  "endpoint": "GET /api/tags/:tag/topics",
  "description": "Tag topics list with explicit page=2, perPage=5",
  "request": {
    "method": "GET",
    "path": "/api/tags/<TAG>/topics",
    "params": { "tag": "<TAG>" },
    "query": { "page": 2, "perPage": 5 }
  },
  "expected": {
    "status": 200,
    "contentType": "application/json",
    "body": {
      "topics": "<ARRAY>",
      "source": "nodebb",
      "totalCount": "<NON_NEG_INT>",
      "page": 2,
      "perPage": 5
    },
    "bodySchema": {
      "type": "object",
      "required": ["topics", "source", "totalCount", "page", "perPage"],
      "properties": {
        "topics": {
          "type": "array",
          "items": { "$ref": "#/$defs/TagTopicItem" }
        },
        "source": { "type": "string", "enum": ["nodebb", "fallback"] },
        "totalCount": { "type": "integer", "minimum": 0 },
        "page": { "type": "integer", "const": 2 },
        "perPage": { "type": "integer", "const": 5 }
      },
      "additionalProperties": false,
      "$defs": {
        "TagTopicItem": {
          "type": "object",
          "required": ["tid", "uid", "cid", "title", "slug", "mainPid", "postcount", "viewcount", "timestamp"],
          "properties": {
            "tid": { "type": "integer", "minimum": 1 },
            "uid": { "type": "integer", "minimum": 1 },
            "cid": { "type": "integer", "minimum": 1 },
            "title": { "type": "string" },
            "slug": { "type": "string" },
            "mainPid": { "type": "integer", "minimum": 1 },
            "postcount": { "type": "integer", "minimum": 0 },
            "viewcount": { "type": "integer", "minimum": 0 },
            "timestamp": { "type": "integer", "minimum": 0 }
          },
          "additionalProperties": false
        }
      }
    }
  },
  "notes": "Verifies that explicit pagination params are reflected in the response envelope."
}
