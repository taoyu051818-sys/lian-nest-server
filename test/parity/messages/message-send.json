{
  "id": "message-send",
  "endpoint": "POST /api/messages",
  "description": "Authenticated user sends a message to another user",
  "request": {
    "method": "POST",
    "path": "/api/messages",
    "body": {
      "toUid": 2,
      "content": "Hello"
    }
  },
  "expected": {
    "status": 201,
    "contentType": "application/json",
    "body": "<MessageResponseDto>",
    "bodySchema": {
      "$ref": "#/$defs/MessageResponseDto"
    },
    "$defs": {
      "MessageResponseDto": {
        "type": "object",
        "required": ["messageId", "fromUid", "toUid", "content", "timestamp", "read"],
        "properties": {
          "messageId": { "type": "string" },
          "fromUid": { "type": "integer", "minimum": 1 },
          "toUid": { "type": "integer", "minimum": 1 },
          "content": { "type": "string" },
          "timestamp": { "type": "string", "format": "date-time" },
          "read": { "type": "boolean" }
        },
        "additionalProperties": false
      }
    }
  },
  "notes": "Currently stubbed: throws NotImplementedException (501) until NodeBB message provider is wired. fromUid is extracted from JWT sub claim. roomId is optional."
}
