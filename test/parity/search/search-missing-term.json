{
  "id": "search-missing-term",
  "endpoint": "GET /api/search",
  "description": "Search without term param returns 400 Bad Request",
  "request": {
    "method": "GET",
    "path": "/api/search",
    "query": {}
  },
  "expected": {
    "status": 400,
    "contentType": "application/json",
    "bodySchema": {
      "type": "object",
      "required": ["statusCode", "message"],
      "properties": {
        "statusCode": { "type": "integer", "enum": [400] },
        "message": { "type": "string" },
        "error": { "type": "string" }
      }
    }
  },
  "notes": "Missing term query param should trigger BadRequestException."
}
